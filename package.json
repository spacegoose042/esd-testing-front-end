{
  "name": "esd-testing-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port ${PORT:-5000}",
    "lint": "eslint .",
    "web": "npm run preview"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
